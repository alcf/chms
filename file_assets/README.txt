Make sure that the file_assets directory as well as all subdirectories and files are set to 777 (read, write and executable by all)

Any folders created here by the application should be explicitly set to be ignored in .gitignore!